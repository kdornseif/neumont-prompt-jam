Prompt 1: data-entry application 

Prompt 2: I want to build a simple and easy to understand Python data-entry application for personal notes. Start with a terminal based version only. I will take your response and put it directly into VS Code, so please indicate the file name for code and do full file every time, not changes only, so that I can paste over the file contents without the risk of copy-paste mutation. Include the ability to add a note, list the notes taken, and remove a note. There does not need to be any data persistence yet, the notes can exist within the session. Start with a small set of 1-3 notes on application start. Include detailed comments in line with the code on its purpose, and a summary markdown on all of the capabilities of the application. Include a readme with instructions on how to run the application and install any pre-requisites on the machine. Update the summary.md and readme.md with every prompt. 

Prompt 3: I'd like to get an end-to-end application in place before adding additional note related features. I would like to keep the CLI functionality to add notes and introduce a web interface with similar functionality. The CLI and web interface should leverage the same logic and data as much as possible, anywhere they differ should be called out explicitly in the summary.md. I do not have a preference over the web framework used, choose a stack that is popular, easy to understand and easy to maintain. Provide full files for any code changes to reduce the possibility of drift. For the data I would like the option to load and persist from a .csv on the local machine. The user will need to be able to specify a way of loading data (there will only be one way to start, via csv, but more later) and based on the method provide the necessary information to load from a source. Once the source is loaded, it should be possible to persist back to the source. At any point the user should be able to change the source/target and "re-load" or "save-as". 

Prompt 4: For the next iteration add tests so that regressions in functionality are caught as new features are added. The test suite should cover all explicit functionality and include common failure scenarios, if there is a well-known and lightweight guideline around appropriate number and style of tests reference it in the testing files and README.md and follow it. The goal is still to keep things simple and maintainable, so stack and structure choices are open. The test suite(s) should cover core functionality as well as CLI and web interactions. Create a release_notes.md document with instructions that it is append only and includes a version update description of what Functional and Non-Functional features have been introduced or changed in each version change. I'm not familiar with best practices for versioning files, but I'd like to have some correlation between the code and the release notes of what files have changed with each version, that can be seen on the files and the release notes. I am open to following a popular pattern that is simple and maintainable on release notes as well, the source is also tracked in git and has an associated github project. Provide full files for any code changes to reduce the possibility of drift. I'd like to add a feature in the next prompt, but in your response include any thoughts on things that should be done to complete a simple "end-to-end" personal application (no need for login or multi-user support) before getting into more functionality. 

Prompt 5: The tests encountered an error when running pytest, which I've included. I've also included a screenshot of the directory structure, the contents of the test files are copied exactly and pytest is installed in the virtual environment. The next version should focus on getting the tests running without any other changes. Provide the release_notes.md as a single copy and pastable section of content the same as the other code files (not changes only, despite the append only instruction I will still be overwriting the whole file). Provide the summary.md in this format as well (single copy and paste block, similar to code file). 